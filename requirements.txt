fastapi>=0.115.0
uvicorn>=0.30.6
python-multipart>=0.0.9
python-dotenv>=1.0.1
pydantic>=2.12.0
pytest>=8.3.3
httpx>=0.27.2
PyJWT>=2.10.0  # JWT verification for Supabase access tokens (backend/app/auth.py)
cryptography>=43.0.0  # ES256 signature verification for Supabase's rotating ECC signing keys (backend/app/auth.py)
supabase>=2.0.0  # Document persistence (backend/app/supabase_service.py); degrades gracefully if absent
google-generativeai>=0.3.0  # For Gemini fallback provider
groq>=0.12.0  # Groq provider (OpenAI-compatible SDK)
paddlepaddle>=2.6.0  # NOTE: Not available for Python 3.14 yet; requires Python <=3.12
paddleocr>=2.9.0
# --- Compatibility notes ---
# imgaug==0.4.0 is a transitive dep of paddleocr.
# It uses np.bool/np.complex removed in numpy 2.0.
# backend/app/ocr.py restores these aliases at import time.
# OneDNN (MKL-DNN) is disabled via FLAGS_use_mkldnn=0 in ocr.py
# to avoid fused_conv2d NotFoundError on some CPU backends.
PyMuPDF>=1.25.0
Pillow>=10.0.0
numpy>=1.24.0
