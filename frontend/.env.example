# Local FastAPI backend. Keep this value for local frontend development.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# In Vercel, set NEXT_PUBLIC_API_BASE_URL in the project environment to the
# deployed backend origin instead of committing that deployment URL here.